@model IEnumerable<HotelBookingWebsite.Models.InventoryModel>

<table class="table">
    <tr>
        <th>
            Room Type
        </th>
        <th>
            Total Amount 
        </th>
        <th>
            Occupancy
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Inventory)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rate)
        </td>
    </tr>
}

</table>
